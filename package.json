{
  "name": "investment-portfolio",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "npm run dev -w @repo/web",
    "build": "npm run build -w @repo/web",
    "lint": "npm run lint -w @repo/web"
  }
}
